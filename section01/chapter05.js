// 1. Number Type
let num1 = 27;
let num2 = 1.5;
let num3 = -20;

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN;

// 2. String Type
let myName = "김재환";
let myLocation = "인천";
let introduce = myName + myLocation;

// 템플릿 리터럴 문법
let introduceText = `${myName}은 ${myLocation}에 거주합니다.`;

// 3. Boolean Type
let isSwitchOn = true;
let isEmpty = false;

// 4. Null Type (아무것도 없다, 직접 변수에 명시적으로 할당해야함)
let empty = null;

// 5. Undefined Type (변수를 초기화하지 못했거나, 불가능한 값을 불러오려고 할 때 )
let none;
console.log(none);
