// 1. 변수
let age;

age = 30;

// 2. 상수(초기화를 해야만함)
const birth = "1993.11.10";

// 3. 변수 명명규칙(네이밍 규칙)
// 3-1. $, _ 제외한 기호는 사용할 수 없다.

let $_name;

// 3-2. 숫자로 시작할 수 없다.
let name1;

// 3-3. 예약어를 사용할 수 없다.

// 4. 변수 명명 가이드(누구라도 보고 알 수 있게)
let a = 1;
let b = 1;
let c = a - b;

let salesCount = 1;
let refundCount = 1;
let totalSalesCount = salesCount + refundCount;
